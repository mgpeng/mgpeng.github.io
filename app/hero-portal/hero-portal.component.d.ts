import { Router } from '@angular/router';
import { Title } from '@angular/platform-browser';
export declare class HeroPortalComponent {
    private _rtr;
    private _t;
    title: string;
    constructor(_rtr: Router, _t: Title);
}
